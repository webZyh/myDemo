问题：
1、关于this和setter方法
构造函数中：
public Person(String name){
  this.name = name;
  this.setName(name); 这两种设置name属性的区别？
}

2、关于对象多态性
向上转型  A a = new B()
a.print() 是调用了子类中复写父类的print()
那么直接实例化B对象，调用自己的print()不是也可以吗？